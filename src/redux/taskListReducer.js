let initialState = {
    data: [{title: 'important'}, {title: 'weekend'}],
    items: [{taskTitle: 'print the documents'}]
}

const taskListReducer = (state = initialState, action) => {
    return state;
}
export default taskListReducer;